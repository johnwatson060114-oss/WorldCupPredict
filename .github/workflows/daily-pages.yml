name: Daily forecast and GitHub Pages

on:
  workflow_dispatch:
  schedule:
    # 05:00 UTC = 13:00 Asia/Shanghai. Scheduled updates start on 2026-06-17 Beijing time.
    - cron: '0 5 * * *'
  push:
    branches: [main, master]
    paths-ignore:
      - 'public/data/history/**'

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
          cache: pip
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: pip install -r requirements.txt
      - run: npm install
      - name: Check Beijing schedule start
        id: schedule_start
        if: github.event_name == 'schedule'
        shell: python
        run: |
          import os
          from datetime import date, datetime
          from zoneinfo import ZoneInfo

          today = datetime.now(ZoneInfo('Asia/Shanghai')).date()
          enabled = today >= date(2026, 6, 17)
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'enabled={str(enabled).lower()}\n')
      - name: Check for a recent local forecast
        id: forecast_freshness
        if: github.event_name == 'schedule' && steps.schedule_start.outputs.enabled == 'true'
        shell: python
        run: |
          import json
          import os
          from datetime import datetime, timedelta
          from pathlib import Path
          from zoneinfo import ZoneInfo

          path = Path('public/data/daily-forecast.json')
          recent = False
          if path.exists():
              payload = json.loads(path.read_text(encoding='utf-8'))
              timezone = ZoneInfo('Asia/Shanghai')
              now = datetime.now(timezone)
              generated = datetime.fromisoformat(payload['generatedAt']).astimezone(timezone)
              recent = now - generated <= timedelta(minutes=30)
              recent = recent and payload.get('targetDate') == (now.date() + timedelta(days=1)).isoformat()
          with open(os.environ['GITHUB_OUTPUT'], 'a', encoding='utf-8') as output:
              output.write(f'recent={str(recent).lower()}\n')
      - name: Generate next Beijing-day forecast
        if: github.event_name == 'workflow_dispatch' || (github.event_name == 'schedule' && steps.schedule_start.outputs.enabled == 'true' && steps.forecast_freshness.outputs.recent != 'true')
        env:
          API_FOOTBALL_KEY: ${{ secrets.API_FOOTBALL_KEY }}
          FOOTBALL_DATA_TOKEN: ${{ secrets.FOOTBALL_DATA_TOKEN }}
        run: |
          python -m pipeline.settle
          python -m pipeline.generate --archive
          python -m pipeline.strategy_history
      - name: Refresh total-goals model review
        if: github.event_name == 'workflow_dispatch' || (github.event_name == 'schedule' && steps.schedule_start.outputs.enabled == 'true' && steps.forecast_freshness.outputs.recent != 'true')
        run: |
          python artifacts/total-goals-backtest/optimize_and_predict_2026.py > artifacts/total-goals-backtest/last_optimization_run.log
      - run: python -m pytest -q
      - run: npm run build
      - name: Commit forecast history
        if: github.event_name == 'workflow_dispatch' || (github.event_name == 'schedule' && steps.schedule_start.outputs.enabled == 'true' && steps.forecast_freshness.outputs.recent != 'true')
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data
          git diff --cached --quiet || git commit -m "data: archive daily forecast [skip ci]"
          git push
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
